# dashboard

The real SOP for this command lives at [`../../skills/dashboard.md`](../../skills/dashboard.md).
Read that file now and follow it exactly. This stub exists only so Claude Code /
Copilot CLI auto-discover `dashboard` as an invocable command — `skills/` is the
canonical, agent-agnostic home; edit the SOP there, not here.
