# note

The real SOP for this command lives at [`../../skills/note.md`](../../skills/note.md).
Read that file now and follow it exactly. This stub exists only so Claude Code /
Copilot CLI auto-discover `note` as an invocable command — `skills/` is the
canonical, agent-agnostic home; edit the SOP there, not here.
