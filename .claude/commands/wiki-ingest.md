# wiki-ingest

The real SOP for this command lives at [`../../skills/wiki-ingest.md`](../../skills/wiki-ingest.md).
Read that file now and follow it exactly. This stub exists only so Claude Code /
Copilot CLI auto-discover `wiki-ingest` as an invocable command — `skills/` is the
canonical, agent-agnostic home; edit the SOP there, not here.
