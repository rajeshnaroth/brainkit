# wiki-maintain

The real SOP for this command lives at [`../../skills/wiki-maintain.md`](../../skills/wiki-maintain.md).
Read that file now and follow it exactly. This stub exists only so Claude Code /
Copilot CLI auto-discover `wiki-maintain` as an invocable command — `skills/` is the
canonical, agent-agnostic home; edit the SOP there, not here.
