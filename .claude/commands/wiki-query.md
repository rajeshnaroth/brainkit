# wiki-query

The real SOP for this command lives at [`../../skills/wiki-query.md`](../../skills/wiki-query.md).
Read that file now and follow it exactly. This stub exists only so Claude Code /
Copilot CLI auto-discover `wiki-query` as an invocable command — `skills/` is the
canonical, agent-agnostic home; edit the SOP there, not here.
